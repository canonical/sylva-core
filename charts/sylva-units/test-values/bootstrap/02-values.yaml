# Overrride lookup otherwise management-sylva-units unit wouldn't be valid
# as it has to define a repo or a sourceRef.

units:
  management-sylva-units:
    helmrelease_spec:
      chart:
        spec:
          sourceRef:
            kind: GitRepository
            name: sylva-units
            namespace: sylva-system

