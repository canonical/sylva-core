apiVersion: v1
kind: Secret
metadata:
  name: backup-store-s3-${CRONJOB_NAME}
  namespace: ${NAMESPACE:-sylva-system}
  labels:
    app: backup
data:
  S3_ACCESS_KEY: ${S3_ACCESS_KEY}
  S3_SECRET_KEY: ${S3_SECRET_KEY}
