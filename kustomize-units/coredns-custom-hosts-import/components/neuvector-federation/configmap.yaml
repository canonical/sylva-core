---
apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom-${CLUSTER_NAME}
  namespace: "${NAMESPACE:-kube-system}"
  labels:
    sylva.org/coredns-custom-hosts-import: "true"
data:
  hosts: |
    ${CLUSTER_NAME}.hosts ${CLUSTER_NAME}.${CLUSTER_DOMAIN} {
        hosts {
            ${CLUSTER_VIRTUAL_IP} ${NEUVECTOR_FEDERATION_MANAGED_HOSTNAME}
            fallthrough
        }
        whoami
    }

