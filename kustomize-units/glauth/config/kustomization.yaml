---
apiVersion: kustomize.config.k8s.io/v1beta1
configMapGenerator:
  - files:
      - base-groups.cfg
    name: groups
generatorOptions:
  disableNameSuffixHash: true
kind: Kustomization
namespace: glauth
resources:
  - secret.yaml
