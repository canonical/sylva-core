---
apiVersion: v1
kind: Service
metadata:
  name: glauth
  namespace: glauth
spec:
  ports:
  - name: tcp-ldap
    port: 389
    protocol: TCP
    targetPort: ldap
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: glauth
    app.kubernetes.io/name: glauth
