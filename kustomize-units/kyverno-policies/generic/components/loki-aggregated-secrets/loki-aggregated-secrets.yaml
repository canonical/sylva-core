apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: loki-aggregated-secret
  namespace: sylva-system
  annotations:
    kustomize.toolkit.fluxcd.io/force: Enabled
    policies.kyverno.io/title: Aggreate loki credentials
    policies.kyverno.io/minversion: 1.12.0
    policies.kyverno.io/description: >-
      Aggregate loki credentials from various (workload) cluster namespaces
      into a secret that is usable by loki HelmRelease
    sylva.org/kyverno-exclude-kube-system: add-matchCondition
spec:
  generateExisting: true
  webhookConfiguration:
    matchConditions:
    - name: is-loki-aggregated-secret
      expression: "has(object.metadata) && has(object.metadata.labels) && 'sylva.io/aggregated-secret' in object.metadata.labels && object.metadata.labels['sylva.io/aggregated-secret'] == 'loki'"
  rules:
  - name: loki-aggregated-secret
    match:
      any:
      - resources:
          kinds:
          - Secret
          selector:
            matchLabels:
              sylva.io/aggregated-secret: loki
    context:
    - name: tenantList
      apiCall:
        urlPath: "/api/v1/secrets?labelSelector=sylva.io/aggregated-secret=loki"
        jmesPath: "items[].data.{name: base64_decode(username), password:base64_decode(password)}"
      # Build a config that matches the chart values format, so that secret can be merged as is
      # to the chart values (without specifying targetPath, otherwise values would be injected as a string)
      # Secret content will have the following structure:
      #   loki:
      #     tenants:
      #     - name: management-cluster
      #       password: foo
      #     - name: test-workload1
      #       password: bar
    - name: lokiConfig
      variable:
        value:
          loki:
            tenants: "{{ tenantList }}"
    generate:
      synchronize: true
      apiVersion: v1
      kind: Secret
      name: loki-secrets
      namespace: sylva-system
      data:
        type: Opaque
        data:
          secrets: "{{ lokiConfig | to_string(@) | base64_encode(@) }}"
---
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: reconcile-loki-helmrelease
  namespace: sylva-system
  annotations:
    kustomize.toolkit.fluxcd.io/force: Enabled
    policies.kyverno.io/title: Reconcile sylve-units HelmReleases
    policies.kyverno.io/minversion: 1.12.0
    policies.kyverno.io/description: >-
      Reconcile loki HelmRelease when loki-secrets is updated
    sylva.org/kyverno-exclude-kube-system: add-matchCondition
spec:
  mutateExistingOnPolicyUpdate: true
  webhookConfiguration:
    matchConditions:
    - name: is-loki-aggregated-secret
      expression: "has(object.metadata) && object.metadata.namespace == 'sylva-system' && object.metadata.name == 'loki-secrets'"
  rules:
  - name: reconcile-loki-helmrelease
    skipBackgroundRequests: false # Required here as we match a resource generated by background controller in previous policy
    match:
      any:
      - resources:
          kinds:
          - Secret
          namespaces:
          - sylva-system
          names:
          - loki-secrets
    mutate:
      targets:
      - apiVersion: helm.toolkit.fluxcd.io/v2
        kind: HelmRelease
        name: loki
        namespace: sylva-system
      patchStrategicMerge:
        metadata:
          annotations:
            reconcile.fluxcd.io/requestedAt: "{{ time_now_utc() }}"
            kyverno.reconcile-loki-helmrelease/requestedAt: "{{ time_now_utc() }}"
