---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${SERVICE}-tls
spec:
  secretName: grafana-general-tls
  issuerRef:
    name: certificate-issuer
    kind: ClusterIssuer
  commonName: ${GRAFANA_DNS}
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  emailAddresses:
  - surname.name@sylva.org
  subject:
    organizations:
    - Sylva
    countries:
    - eu
    organizationalUnits:
    - DEV
  dnsNames:
  - ${GRAFANA_DNS}
  - localhost
  ipAddresses:
  - 127.0.0.1
