apiVersion: openidclient.keycloak.crossplane.io/v1alpha1
kind: ClientDefaultScopes
metadata:
  name: harbor-groups-client-scope
  annotations:
    crossplane.io/external-name: builtin-sylva-realm-role-offline-access
spec:
  forProvider:
    clientIdRef:
      name: harbor
      policy:
        resolution: Required
        resolve: Always
    defaultScopes:
      - groups
      - profile
      - offline-access
      - email
    realmId: sylva
  providerConfigRef:
    name: keycloak-provider-config
