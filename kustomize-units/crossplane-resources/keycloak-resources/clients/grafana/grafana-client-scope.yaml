apiVersion: openidclient.keycloak.crossplane.io/v1alpha1
kind: ClientDefaultScopes
metadata:
  name: grafana-groups-client-scope
spec:
  forProvider:
    clientIdRef:
      name: grafana
      policy:
        resolution: Required
        resolve: Always
    defaultScopes:
      - groups
      - profile
      - openid
      - roles
    realmId: sylva
  providerConfigRef:
    name: keycloak-provider-config
