apiVersion: crd.projectcalico.org/v1
kind: GlobalNetworkSet
metadata:
  labels:
    role: allowed-to-kubeapi
  name: allowed-public-subnets-to-kubeapi
spec:
  nets: ${ALLOWED_PUBLIC_SUBNETS_TO_KUBEAPI}
