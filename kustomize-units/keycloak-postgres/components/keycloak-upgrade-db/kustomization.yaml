apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - patch: |
      - op: add
        path: /spec/bootstrap
        value:
          initdb:
            import:
              type: microservice
              databases:
                - keycloak
              source:
                externalCluster: keycloak-old-db
      - op: add
        path: /spec/externalClusters
        value:
          - name: keycloak-old-db
            connectionParameters:
              host: postgres-primary
              user: postgres
              dbname: keycloak
            password:
              name: postgres
              key: postgres-password
    target:
      kind: Cluster
      name: cnpg-keycloak
      namespace: keycloak

