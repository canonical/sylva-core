---
apiVersion: v1
kind: Service
metadata:
  name: cluster-vip
  namespace: kube-system
  annotations:
    metallb.io/allow-shared-ip: cluster-external-ip
    metallb.io/loadBalancerIPs: ${allocated_ip}
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  type: LoadBalancer
  loadBalancerClass: "${lb_class}"
  ports:
  - name: rke2-proxy
    port: 54321
    protocol: TCP
    targetPort: 54321
  selector:
    component: kube-apiserver
