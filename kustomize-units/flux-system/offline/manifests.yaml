# We want to support offline deployments for flux, but don't want to maintain a copy of flux manifests here
#
# So this file is just a placeholder for manifests that will be generated by flux client during bootstrap phase
kubectl apply -f https://github.com/fluxcd/helm-controller/releases/download/v0.37.4/helm-controller.yaml
