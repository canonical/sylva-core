apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flux-sources-pvc
  namespace: flux-system
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${SOURCE_STORAGE_CLASS}
  resources:
    requests:
      storage: ${SOURCE_STORAGE_SIZE}
