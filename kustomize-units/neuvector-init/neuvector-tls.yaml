---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: neuvector-tls
  namespace: neuvector
spec:
  secretName: neuvector-tls
  issuerRef:
    name: certificate-issuer
    kind: ClusterIssuer
  commonName: ${NEUVECTOR_DNS}
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  emailAddresses:
  - surname.name@sylva.org
  subject:
    organizations:
    - Sylva
    countries:
    - eu
    organizationalUnits:
    - DEV
  dnsNames:
  - ${NEUVECTOR_DNS}
  - localhost
  ipAddresses:
  - 127.0.0.1
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: neuvector-mastersvc-tls
  namespace: neuvector
spec:
  secretName: neuvector-mastersvc-tls
  # Currently the certificate is trusted by the Sylva CA by referring to the ca-issuer.
  # It is possible to leverage an external authority by referring a vault-issuer or an acme-issuer.
  # In this case, configure the issuer according to the spec of the external authority and update issuerRef below
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  commonName: ${NEUVECTOR_MASTERSVC_DNS}
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  emailAddresses:
  - surname.name@sylva.org
  subject:
    organizations:
    - Sylva
    countries:
    - eu
    organizationalUnits:
    - DEV
  dnsNames:
  - ${NEUVECTOR_MASTERSVC_DNS}
  - localhost
  ipAddresses:
  - 127.0.0.1
