---
cluster:
  capi_providers:
    bootstrap_provider: cabpk

  kubeadm:
    users:
      - name: sylva-user
        groups: users,sylva-ops
        sudo: ALL=(ALL) NOPASSWD:ALL
        shell: /bin/bash
        lockPassword: false
        passwd: $1$pdWEicWs$9ZWQf5.CWyXccmP8Chuu01 # sylva


## Expected OS image must be configured here

  # capm3:
  #   os_image_selector:
  #     os: ubuntu

# configure containerd registry mirrors for docker hub
registry_mirrors:
  hosts_config:
    docker.io:
    - mirror_url: https://mirror.gcr.io
