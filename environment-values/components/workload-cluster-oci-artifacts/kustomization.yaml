apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

components:
- ../oci-artifacts

patches:
  - target:
      kind: OCIRepository
      name: sylva-units
    patch: |
      - op: replace
        path: /metadata/namespace
        value: WORKLOAD_CLUSTER_NAMESPACE
