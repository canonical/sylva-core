apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

components:
- ../oci-artifacts

patches:
  - target:
      kind: SylvaUnitsRelease
      name: sylva-units
    patch: |
      - op: replace
        path: /spec/clusterType
        value: workload
