apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

namespace: sylva-units-preview

patches:
- patch: |-
    kind: SylvaUnitsRelease
    metadata:
      name: sylva-units
    spec:
      clusterType: preview
