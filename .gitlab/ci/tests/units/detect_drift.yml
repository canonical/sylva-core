detect-drift:
  script:
    - kubectl logs -l app=helm-controller -n flux-system > helm_controller.log
    - ./tools/detect_drift.sh helm_controller.log

mgmt-detect-drift:
  extends:
    - .test-base-management-cluster
    - .detect-drift
  variables:
    DESCRIPTION: Check drifts in the mgmt cluster
